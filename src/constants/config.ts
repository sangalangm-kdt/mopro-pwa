export const APP_CONFIG = {
  APP_NAME: "QR Scanner App",
  VERSION: "1.0.0",
};

export const SCAN_RESULT_CONFIG = {
  EXPAND_THRESHOLD: -30, // Swipe up to expand
  COLLAPSE_THRESHOLD: 80, // Swipe down to collapse
};
